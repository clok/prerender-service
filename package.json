{
  "name": "prerender-service",
  "version": "1.0.0",
  "description": "Common Prerender service for Deis",
  "main": "server.js",
  "dependencies": {
    "prerender": "^4.0.5",
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  }
}
